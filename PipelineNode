#!/usr/bin/env groovy
node {
    checkout scm    
    stage ('Build') {
        withMaven ( maven: 'Maven Test) {
                   sh 'mvn compile'
                   }
    }   
    stage ('Test') {
        sh 'mvn test'
    }
    stage ('Deploy') {
        sh 'mvn package'
    }
}
