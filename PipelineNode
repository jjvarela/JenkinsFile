#!/usr/bin/env groovy
node {
    checkout scm    
    stage ('Build') {
        sh 'mvn compile'
    }
    stage ('Test') {
        sh 'mvn test'
    }
    stage ('Deploy') {
        sh 'mvn package'
    }
}
